# Сызба тізімі · 图的待办清单

Owner 2026-09-23 在 `_figs.html` 样张上逐张批注出来的 14 处。**此文件是待办，不是规范** —— 改完一条就划掉一条，全部清空后删除本文件。规范在 `ROUTE_CONVENTION.md`。

重跑样张：`node tests/figsheet.js`（需要 jsdom）。

---

## A · 图把答案送出去 —— 最优先，这是会教错的

- [ ] **`numberline` · FR-04** ✔已在代码中确认
  每个刻度下面都印着 `1/6 2/6 3/6 4/6 5/6`，题目问「点在哪个分数上」，孩子照着点下面的标签念就行。
  出处：`fr/figs.js` 的 `numberline()`，`f.showLabels` 那一支打印 `${i}/${d}`。
  方向：出题的那一张不许带刻度标签；标签只能出现在讲解/提示图里。

- [ ] **`bar_compare` · WP-04**（「Тимур 3 есе көп」，答案 = 6）
  Үлкені 那几格里的东西是**可数的**，数一遍就是答案。
  2026-09-22 已修的是另一类（`fp` 把「1 бөлік = 答案」直接印出来，见 `wp/bank.js` 的 TP-04-01/03/05）；这一类是**答案 = 总数**，图仍然把总数画成可数的。
  方向：倍数关系图只给长度，不给个数 —— 这正是《减法方程画法》型 V「上条禁止刻度」那条规矩。

## B · 图在说谎 —— 次优先

- [ ] **`bar` · TE-01「8 + x = 18」** ✔已在代码中确认
  x = 10，**比 8 大**，图上却画得比 8 窄一半。
  出处：`core/figs.js` 的 `figBar()` —— 未知段固定给 `Math.max(50, W*0.3)`，已知段按数值缩放。只要 x > 已知数，图就是反的。
  方向：要么未知段按「不小于最大已知段」画并标注「不按比例」，要么整张图明确声明不按比例（《多步方程画法》的做法）。

- [ ] **`array` · WP-03**（「3 个盒子，每盒 6 本」）
  画成了一整块 3×6 的方阵，**盒子没画出来**。孩子看到的是 18 个东西，不是 3 组 6 个。
  方向：3 个明确分开的框，每框 6 个。

## C · 图缺了题目里的关键数字

- [ ] **`unit_bar` · WP-04**（「Сырымда 15 кәмпит，5 есе артық」）
  5 个等格画出来了，但 **15 没标在图上**。

- [ ] **`wrap` · TE-09「(4 + x) − 2 = 7」**
  图里有 4、x、−2，**唯独没有 7**。等号右边不进图，图就只画了半个方程。

## D · 图只是把题干抄了一遍

- [ ] **`short_note` · WP-01**
  «Болды – 60 тг / Жұмсады – 20 тг / Қалды – ? тг» 跟题干一字不差，没有任何新信息。
  方向：要么画成图（条形），要么这一类题不配图。

## E · 「看不出在问什么」—— owner 尚未确认我的理解，动手前先问

- [ ] **`dist` WP-09 / `meet` WP-10 / `same` WP-10**
  我的理解：速度、时间、距离三个量全挤在同一条数轴上（箭头=速度、括号=距离、数字=时间），三者没有视觉区分。**待确认**

- [ ] **`grp` · TE**（4 组 ×3 点，上「?」下「12」）
  我的理解：问的是组数还是每组几个，图上说不清。**待确认**

- [ ] **`bal` · TE-01**
  我的理解：装未知数的杯子太小、跟方块混在一起，看不出哪个是 x。**待确认**

- [ ] **`discs` · AR-30「160 ÷ 4」**
  我的理解：图上只有 160（1 个百 + 6 个十），**÷4 一点没画**，得画成分成 4 堆。**待确认**

- [ ] **`twobars` · FR-02**
  owner 写「看不出来 / 不一样」。是两条带长度不等没法比，还是题干没说清要比什么？**待确认**

- [ ] **`bars` · WP-01「Камила 16 кем」**
  那个虚线「−16」框太小看不清，还是这个画法本身不对？**待确认**

## F · 新图（te/figs2.js，尚未接进路线）

- [ ] **`s1d`** —— 删掉中间那条带「?」的空括号，只留最里的「x + 3」和最外的 30。
