# Орын мәні (PV) · 待办

Owner 2026-09-23 在线上逐关试出来的。**此文件是待办，不是规范。** 改完一条划掉一条，清空后删除。
端到端测试：`node tests/pv.js`（需要 jsdom）。

---

## ★ 两件设计级的，要先定下来再动手

### ① 无图关 —— **已做完**（owner 2026-09-23 定，当天实现）

每个加减关后面跟一个 `…_n` 的无图孪生关：`a1` → `a1_n` → `s1` → `s1_n` → …
**28 个孪生关，PV 从 48 关变 76 关。**

- 孪生关借父关的生成器：`generateQuestion` 在调用期间把 `state.level` 换成父关 id，**没有一个生成器需要知道孪生关的存在**
- 拿掉的只有**教具**：方块、十格图、竖式旁边的圆片表（`.block-display` `.horiz-vis` `.ops-pv-chart`）。
  **竖式本身不拿掉** —— 那是要教的记法，不是拿来看的
- **答错 → 图放回来，这题仍然算错**，连击照样归零（owner：「打错了要给提示图，不过不算对」）
- 下一题又是光的

**编号那个坑填了**：`stageId` 从「LEVEL_ORDER 下标」改成写死的 `STAGE_NO` 表，老 48 关保号，孪生关拿 PV-49…76；
`unlockedUpTo` 是下标，插关后全错位，改成**从 `completed`（按 id 存）重算** —— 顺带自愈将来任何一次插关。
`tests/pv.js` 有 8 项盯着这些（含「老 48 关编号一个没变」）。

### ② 三位数起用 DM 位值圆片表 —— **已接进 16 关**

`a7 a8 a9 a10` · `s4 s5 s6 s7` · `a4d1…a4d4` · `s4d1…s4d4`。**两位数不动**（owner：「两位数不需要」），
它们还是原来那张两行表。

三个记号：花掉的圆片画**虚线**留在原列 · **蓝色弧箭头**指向它去的那一列 · 到达的十个圈在**虚线框**里；
减法**打叉不删除**，加法**只圈十个不圈整列**。

修过一个硬伤：**借出去的圆片如果本身是借来的，没被画成虚线** —— `4072 − 1385` 的百位「收到十个、再借一个给十位」，
那一个混在实心里，图上百位读作 7，答案是 6。现在 `tests/pv.js` 有一条机检把
**「每一列剩下的圆片数 = 答案那一位」**钉死（八个算式，含连续借位和 `2000 − 999`）。

---

## 还没修的具体问题

- [ ] **竖式：答案位数比算式少时，答案行仍然给满格**（owner 圈的 `63 − 58`）
  答案是 5，但答案行有两个输入框，个位那个填完还剩一个十位框。`checkOps` 用 `parseInt(str)||0`，所以留空其实能判对 —— 但孩子面前摆着一个填不掉的框，写 0 又不对。
  方向：答案行的输入框数 = 答案的位数，右对齐在个位那一列下面。

- [x] ~~计数器 1/10 线上仍未动~~ —— 是缓存，强刷后正常
  代码里已修（`id="qCount"`，`generateQuestion` 更新，`tests/pv.js` 有断言）。线上没变**极可能是缓存** —— `pv/index.html` 是页面本身，GitHub Pages 给 HTML 的 `max-age` 是 600 秒，浏览器还会再缓一层。
  下次先**强制刷新**（Ctrl+F5 / 手机上换无痕窗口）再看；还不动就是另有一条不走 `generateQuestion` 的换题路径，需要重查。

---

## 已修（2026-09-25）

- [x] **两位数加减的提示改成三行方块表**（owner 2026-09-25 的笔记本那页：十位 | 个位，a、b、结果三行，换位画出来）。
  只在**提示**里（owner：「要用 1，不过要放进提示里」），题目旁边那张两行表不动。
  加法：个位满十的十个方块淡化、虚线框住，箭头指向十位，十位多出一条虚线框的新条；减法：十位借出的那条淡化留在原位，
  箭头指向个位，到达的十个在虚线框里，减掉的方块**打叉不删除**，没叉的就是答案。第三行随提示的步骤一步步长出来。
  范围：`a3 a4 a5 a6 s2 s3` 及其无图孪生（答错时给的提示）；三位数起仍是圆片表。`tests/pv.js` 第 30–36 项。

## 已修（2026-09-23，已推送）

- [x] 竖式吞 0 —— `aDigits[...] || ''`，数字 0 被当成空字符串。`1004 + 2852` 印成 `1 _ _ 4`
- [x] `c1` 自答题 —— 「9 — бұл қандай сан?」把数字印出来再问这是什么数
- [x] 十格图把算式做完 —— 三处（加法说明、SVG 标签、减法说明）
- [x] 比较关横排挤 —— 改成上下排；`=` 按 owner 决定不加
- [x] `a2` 不进位 —— `while(a+b<=9)` 放进了 `5+5=10`
- [x] `s20b` 的 5 —— `a−b=5` 时「从十里减 5」正好等于答案
- [x] 计数器只在进关卡时画一次
- [x] **等号右边画什么，分两种**（owner 2026-09-23 两次指示合起来的规矩）：
  - **要换位的**（`a2` 凑十、进位加）→ **不画结果**。十格图只到「10 + 3 = ?」为止，最后一步是孩子的活
  - **不换位的**（`a20n` `a3` `a4d1` 这类）→ **画出来**。「17 + 1」的意思就是「十不动、个位合起来」，
    合起来的那堆正是图要说的话；摆个空框等于什么都没说
  （我一度把两种都删了，owner 圈出 `17 + 1` 的空框纠正：「把两个数加起来的数量要画在空位置上，不要空着」）
- [x] **过关改成「连续答对八个」**（owner 2026-09-23）—— 原来是「10 题里对 8 题」，顺序不论、还能重试。现在
  `state.streak` 连对才算，错一次归零；关卡不再固定十题，而是**连够就过、给到 need+12 题为止**。表头改成
  «қатарынан N/8»，结算屏显示最长连击。Түсіну 关是 4 连。`bridge.js` 报给平台的分母改成「实际问了几题」——
  干净的 8/8 = 三星，8 对 13 题过关但零星。
- [x] **十格图上面那条散方块去掉**（owner：只需要下面的）—— 那条画的是「a 蓝 + b 橙 = 全部排一行」，
  第三组就是答案摆出来给人数。凑十法的全部意思在十格图里，上面那条既多余又泄底
- [x] **定位测试对孪生关问「1 + 1 = ?」，然后把孩子送进四位数**（owner：测试阶段老是出这个问题，然后 10000 以内的题也会出）
  `genPlacementQ` 是一串 `if(lv==='c1')…` 的精确匹配，孪生关 id 一个都匹配不上，掉到函数末尾的兜底 ——
  那行正好是 `return {q:'1 + 1 = ?', answer:2}`。它顶着「Азайту, алумен · суретсіз」的标题出现，
  而 `1+1` 谁都答对，梯子于是一路 +3 冲进四位数。两个现象是同一个 bug。
  修：`genPlacementQ` 里解析成父关 id（`genComprehension` 也走这个函数，一并修好）。
  另外梯子改成只走**非孪生的 48 级**（`PLACE_ORDER`）—— 孪生跟父关问的是同一道题，探测它等于白花一级；
  结果再映射回 `LEVEL_ORDER` 位置去解锁，孪生跟着父关一起开。全对现在 **17 题**到顶。
- [x] **数方块改成横排的位值表 + 数位名**（owner：「这些要横着放而且要写上个位十位百位等（一位数不需要）」）
  每个数位一列、左右排（跟数字的读序一致），列头写 Мыңдық/Жүздік/Ондық/Бірлік，方块在**列内**换行。
  一位数不标头。数位是 0 的列**留着、空着、带头** —— 405 的那个 0 这样才看得见。
  间距按被间隔的东西定：百片之间 24、十条 12、个位 7 —— 缝必须大过片内部方格的缝，否则九个百片读成一整块。
- [x] ~~位值表题的方块被拆成上下行~~（被上面这条取代） —— 是我 9-22 那个换行改动的回归。现在**放得下就一行**（左右排，跟表格的列同向），放不下才按数位分行
