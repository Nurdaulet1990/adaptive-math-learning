# Орын мәні (PV) · 待办

Owner 2026-09-23 在线上逐关试出来的。**此文件是待办，不是规范。** 改完一条划掉一条，清空后删除。
端到端测试：`node tests/pv.js`（需要 jsdom）。

---

## ★ 两件设计级的，要先定下来再动手

### ① 每个加减关后面跟一个**无图**的同题关 —— owner 2026-09-23 已定

Owner 原话：**「每一个阶段（加减）后面要跟一个没有图的阶段，比如过了有图片的 10 内的加法接着出现无图的」**
以及 **「打错了要给提示图，不过不算对」**。

定下来的三条：

1. **粒度是成对的。** `a1`（10 以内加法·有图）→ 新增 `a1n`（同样的题·无图）→ `s1` → `s1n` → …
   加减关一共 **28 个**（d1:2 · d2:4 · d3:6 · d4:8 · d5:8），配对后 PV 从 48 关变 **76 关**。
2. **无图关答错 → 把图放出来，但这一题仍然算错。** 图是犯错的**后果**，不是第二次机会：
   连击照样归零。这跟「连续答对八个才过关」是一套 —— 看过图之后要重新连起来。
3. `u1…u5`「Түсіну」关本来就是纯文字的，**不动**；它们是模块末尾的综合检查，跟这里的成对无图关是两回事。

**动手前必须先填的坑 —— 关卡编号。** `bridge.js` 的 `stageId()` 是
`'PV-' + (LEVEL_ORDER.findIndex(...) + 1)`，**编号 = 在数组里的位置**。往中间插 28 个关，
后面每一关的编号全变 —— 而 `ROUTE_CONVENTION` 的铁律是「关卡编号永不重编、永不复用」：
学生存下的进度、所有日志事件都按这个字符串指向关卡。插队会把所有人的历史指到错的站上。

施工顺序：

1. `stageId` 从「位置」改成**显式的 `levelId → PV-nn` 表**：现有 48 关保号，新增 28 关拿 PV-49…PV-76
2. `state.unlockedUpTo` 也是**下标**，同样错位 —— 载入时改成从 `state.completed`（按 id 存，安全）重算
3. 再插关：`MODULES` / `LEVEL_ORDER` 里每个加减关后面跟一个 `…n`
4. 题目加「无图」开关：`genHorizOps` / `genOps` 按关卡决定画不画 `visHTML`
5. `showFeedback` 在无图关答错时把图补出来（`state.streak` 照样归零）
6. `tests/pv.js` 加断言：无图关默认没有 `.block-display`；答错后有；且 `state.streak === 0`

### ② 三位数起，加减改用 DM 的位值圆片表 + 换位箭头 —— **画法已出，等你看**

`pv/discs.js` 已写好，`pv/_discs.html` 是样张页（十个例子，含两个四位数）。**还没接进任何关卡。**

三个记号（照 owner 给的两页原书）：

- 花掉的圆片画**虚线**，留在它原来那一列
- **蓝色弧箭头**指向它去的那一列
- 到达的十个圈在**虚线框**里；加法**只圈十个，不圈整列**
- 减法**打叉不删除** —— 图上始终还看得见原来的数是多少

涉及 16 关：`a7 a8 a9 a10` · `s4 s5 s6 s7` · `a4d1…a4d4` · `s4d1…s4d4`。
现状是 `renderPVCell` + `.pv-chart` 那张表已经在了，但它只摆两个加数，**换位一点没画**。

---

## 还没修的具体问题

- [ ] **竖式：答案位数比算式少时，答案行仍然给满格**（owner 圈的 `63 − 58`）
  答案是 5，但答案行有两个输入框，个位那个填完还剩一个十位框。`checkOps` 用 `parseInt(str)||0`，所以留空其实能判对 —— 但孩子面前摆着一个填不掉的框，写 0 又不对。
  方向：答案行的输入框数 = 答案的位数，右对齐在个位那一列下面。

- [x] ~~计数器 1/10 线上仍未动~~ —— 是缓存，强刷后正常
  代码里已修（`id="qCount"`，`generateQuestion` 更新，`tests/pv.js` 有断言）。线上没变**极可能是缓存** —— `pv/index.html` 是页面本身，GitHub Pages 给 HTML 的 `max-age` 是 600 秒，浏览器还会再缓一层。
  下次先**强制刷新**（Ctrl+F5 / 手机上换无痕窗口）再看；还不动就是另有一条不走 `generateQuestion` 的换题路径，需要重查。

---

## 已修（2026-09-23，已推送）

- [x] 竖式吞 0 —— `aDigits[...] || ''`，数字 0 被当成空字符串。`1004 + 2852` 印成 `1 _ _ 4`
- [x] `c1` 自答题 —— 「9 — бұл қандай сан?」把数字印出来再问这是什么数
- [x] 十格图把算式做完 —— 三处（加法说明、SVG 标签、减法说明）
- [x] 比较关横排挤 —— 改成上下排；`=` 按 owner 决定不加
- [x] `a2` 不进位 —— `while(a+b<=9)` 放进了 `5+5=10`
- [x] `s20b` 的 5 —— `a−b=5` 时「从十里减 5」正好等于答案
- [x] 计数器只在进关卡时画一次
- [x] 加法方块图把结果画出来 —— `renderDualWithTens` 的等号右边整组画全了，可数即答案。改成虚线空框 + ?
- [x] **过关改成「连续答对八个」**（owner 2026-09-23）—— 原来是「10 题里对 8 题」，顺序不论、还能重试。现在
  `state.streak` 连对才算，错一次归零；关卡不再固定十题，而是**连够就过、给到 need+12 题为止**。表头改成
  «қатарынан N/8»，结算屏显示最长连击。Түсіну 关是 4 连。`bridge.js` 报给平台的分母改成「实际问了几题」——
  干净的 8/8 = 三星，8 对 13 题过关但零星。
- [x] **十格图上面那条散方块去掉**（owner：只需要下面的）—— 那条画的是「a 蓝 + b 橙 = 全部排一行」，
  第三组就是答案摆出来给人数。凑十法的全部意思在十格图里，上面那条既多余又泄底
- [x] 位值表题的方块被拆成上下行 —— 是我 9-22 那个换行改动的回归。现在**放得下就一行**（左右排，跟表格的列同向），放不下才按数位分行
